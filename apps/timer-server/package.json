{
    "name": "timer-server",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "dev": "nodemon --exec ts-node src/index.ts",
        "build": "tsc"
    },
    "dependencies": {
        "@repo/queue": "*",
        "@repo/protocol": "*"
    },
    "devDependencies": {
        "@repo/tsconfig": "*",
        "typescript": "^5.0.0",
        "@types/node": "^20.0.0",
        "ts-node": "^10.0.0",
        "nodemon": "^3.0.0"
    }
}